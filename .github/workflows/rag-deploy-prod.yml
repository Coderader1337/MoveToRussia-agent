name: RAG bot — deploy prod

on:
  push:
    branches:
      - master
    paths:
      - "rag/**"
      - ".github/workflows/rag-deploy-prod.yml"
  workflow_dispatch:

concurrency:
  group: rag-deploy-prod
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup SSH key
        run: |
          install -m 700 -d ~/.ssh
          echo "${{ secrets.RAG_VPS_SSH_KEY }}" | tr -d '\r' > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "${{ secrets.RAG_VPS_HOST }}" >> ~/.ssh/known_hosts 2>/dev/null || true
          ssh-keygen -y -f ~/.ssh/deploy_key > /dev/null

      - name: Sync rag/ to VPS (prod)
        run: |
          rsync -avz \
            -e "ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no" \
            --exclude 'venv/' \
            --exclude '.env' \
            --exclude 'data/' \
            --exclude '__pycache__/' \
            --exclude 'deploy/github_actions_deploy_key*' \
            rag/ "${{ secrets.RAG_VPS_USER }}@${{ secrets.RAG_VPS_HOST }}:/opt/movetorussia/rag/"

      - name: Install deps and restart prod bot
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no \
            "${{ secrets.RAG_VPS_USER }}@${{ secrets.RAG_VPS_HOST }}" \
            "find /opt/movetorussia/rag/deploy -name '*.sh' -exec chmod +x {} + && \
             bash /opt/movetorussia/rag/deploy/install-and-restart.sh \
               /opt/movetorussia/rag movetorussia-rag-bot"
